---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spring-boot-op                  # Ingress name
  labels:
    app: spring-boot-op
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "spring-boot-op.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: spring-boot-op               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
